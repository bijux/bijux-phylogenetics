# Tests

The executable test suites live under the package directories in this
repository, especially:

- `packages/bijux-phylogenetics/tests/`
- `packages/phylogenetics/tests/`

This root-level `tests/` directory exists to satisfy the canonical repository
layout and to hold cross-package test assets when the repository grows into that
need.
