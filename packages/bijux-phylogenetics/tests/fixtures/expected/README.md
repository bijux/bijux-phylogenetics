# Expected Outputs

Checked-in golden outputs and deterministic expected artifacts live in this
directory.

The `beast2_strict_yule_posterior/` bundle anchors BEAST posterior reference
checks against one real XML, log, and tree-set fixture, including exact 10%
burn-in consensus and maximum-clade-credibility trees.
