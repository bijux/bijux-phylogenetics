# Datasets

This directory holds small, durable reference datasets that belong to the
repository.

Do not place transient run products or large generated outputs here. Use
`artifacts/` for local runs unless the dataset itself is a governed repository
input.
