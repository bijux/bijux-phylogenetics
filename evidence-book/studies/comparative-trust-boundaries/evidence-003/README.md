# OU identifiability warning bundle

Governed audit of built-in reference cases that should trigger OU identifiability warnings.

Claim surfaces:

- `ou-identifiability-boundaries-detected` — OU identifiability boundary warnings are detected on reference cases (`matched`)

Sources:

- `ou-identifiability-reference-audit` — OU identifiability warning reference audit via `bijux_phylogenetics.comparative.models:audit_ou_identifiability_reference_examples`

Limitations:

- The audit covers known reference warning families and does not claim that every possible OU pathology is enumerated here.
