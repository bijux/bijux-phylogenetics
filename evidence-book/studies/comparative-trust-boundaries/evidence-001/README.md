# Comparative input rejection bundle

Governed expected-failure cases for rootedness, complete branch lengths, and numeric-response requirements in PGLS workflows.

Claim surfaces:

- `comparative-input-rejection-governed` — Comparative input rejection remains explicit and reproducible (`matched`)

Sources:

- `pgls-input-rejection-guards` — PGLS rootedness, branch-length, and numeric-response guards via `bijux_phylogenetics.comparative.pgls:inspect_pgls_inputs`

Limitations:

- This bundle audits representative rejection cases, not every possible malformed comparative input.
